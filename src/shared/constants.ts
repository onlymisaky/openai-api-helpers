export const DEFAULT_MODEL = 'gpt-4.1-mini';

export const JSON_ONLY_SYSTEM_PROMPT
  = 'You must respond with a valid JSON object only. Do not include markdown, code fences, commentary, or any extra text.';
